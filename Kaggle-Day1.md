# Kaggle 特別講演　Day-1

## Kaggleとは

- Kaggleは、データサイエンスのコンペティションプラットフォームであり、世界中のデータサイエンティストや機械学習エンジニアが集まるコミュニティです。ユーザーはデータセットを使ってモデルを構築し、予測精度を競い合います。


## Kaggleで用いる機械学習
- Kaggleで競うのは、機械学習モデルの予測精度である。
- 機械学習は、学習方法によって大きく分けて3種類に分類される。
  1. 教師あり学習（Supervised Learning）
  2. 教師なし学習（Unsupervised Learning）
  3. 強化学習（Reinforcement Learning）

### 教師あり学習（Supervised Learning）
- 教師あり学習は、入力データとそれに対応する正解ラベル（ターゲット）を用いてモデルを学習させる方法です。

- 特徴量を入力として、目的変数を予測するモデルを構築します。例えば、住宅価格の予測や画像分類などが該当します。

### 教師なし学習（Unsupervised Learning）
- 教師なし学習は、正解ラベルが存在しないデータを用いて、データの構造やパターンを見つける方法です。

- クラスタリングや次元削減などが該当します。例えば、顧客の購買行動の分析や、データの可視化などが該当します。

## 単語

- **特徴量（Feature）**: モデルの入力となるデータの属性や変数のこと。
- **説明変数（Explanatory Variable）**: 予測に使用される変数のこと。
- **目的変数（Target Variable）**: 予測したい結果やラベルのこと。
- **モデル（Model）**: データから学習し、予測を行うための数学的な関数やアルゴリズムのこと。
- **学習（Training）**: モデルにデータを与えてパターンを学習させるプロセスのこと。
- **予測（Prediction）**: 学習したモデルを使って新しいデータに対して結果を推定すること。
- **精度（Accuracy）**: モデルの予測がどれだけ正確であるかを示す指標のこと。
- **過学習（Overfitting）**: モデルが訓練データに対して過剰に適合し、新しいデータに対しては性能が低下する現象のこと。
- **汎化性能（Generalization Performance）**: モデルが未知のデータに対してどれだけ良い予測を行えるかを示す指標のこと。
- **交差検証（Cross-Validation）**: データを複数の部分に分割し、モデルの性能を評価する手法のこと。
- **ハイパーパラメータ（Hyperparameter）**: モデルの学習プロセスに影響を与える設定値のこと。
- **解像度（Resolution）**: データの細かさや精度のこと。
- **多様性（Diversity）**: データやモデルのバリエーションのこと。
- **量(Quantity)**: データの量や規模のこと。
- **欠損値（Missing Value）**: データセットにおいて、値が存在しない状態のこと。
- **外れ値（Outlier）**: データの中で他の値から大きく逸脱している値のこと。
- **正規化（Normalization）**: データのスケールを統一する処理のこと。
- **標準化（Standardization）**: データを平均0、分散1に変換する処理のこと。
- **特徴量エンジニアリング（Feature Engineering）**: モデルの性能を向上させるために、データから新しい特徴量を作成するプロセスのこと。
- **AUC（Area Under the Curve）**: ROC曲線の下の面積を指し、分類モデルの性能を評価する指標のこと。
- **MCC（Matthews Correlation Coefficient）**: 二値分類の性能を評価する指標で、真陽性、真陰性、偽陽性、偽陰性の4つの要素を考慮して計算される.
- **F1スコア（F1 Score）**: 精度と再現率の調和平均を表す指標で、分類モデルの性能を評価するために使用される。
- **One-Hot Encoding**: カテゴリカル変数を二進数のベクトルに変換する手法で、各カテゴリを独立した特徴量として表現する方法。
- **Label Encoding**: カテゴリカル変数を整数に変換する手法で、各カテゴリに一意の整数を割り当てる方法。
- **アンサンブル学習（Ensemble Learning）**: 複数のモデルを組み合わせて予測を行う手法で、個々のモデルの弱点を補完し、全体の性能を向上させることができる。
- 


